name: dbt_practice_dev
version: 1.0.0
config-version: 2

profile: dbt_practice_dev

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  dbt_practice_dev:
    +materialized: table